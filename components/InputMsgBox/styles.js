/*
Styles for the Input Message Box
*/

import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        width: 280,
        borderRadius: 5,
        justifyContent: "flex-start",
        alignItems: "left",
        marginBottom: 20,
        paddingTop: 5,
    },
    buttonText: {
        color: "#FFAA33",
        fontSize: 14,
        fontWeight: "600",
    },
});
